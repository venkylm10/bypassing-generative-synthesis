# GAN weights excluded from GitHub

`last.pt` and `best.pt` (UMM-GAN generator+discriminator checkpoints,
~170MB each) are excluded from this push — GitHub's hard file-size limit is
100MB. Both are present as deliverables at
`/workspace/output/code/weights_gan/` on the experiment pod/GCS.
